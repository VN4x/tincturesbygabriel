# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
.output
.vinxi
.tanstack/**
.nitro
*.local

# Wrangler / Cloudflare
.wrangler/
.dev.vars

# Vercel CLI link (project.json is created after `vercel link` / claim)
.vercel/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Book source — never publish the EPUB or the full extract.
# Anonymous /lugemine still uses the committed sample JSON (getSample).
# Full text on Lovable lives in private book_sections; do not gitignore the sample.
*.epub
*.pdf
*.af
private/
src/content/book-et.full.json

# Local secrets — origin/main tracks a Lovable .env; never take that into this branch
.env
.env.*
!.env.example

# Clean Lovable snapshot — never merge origin/main into this working tree as a sync
/lovable/

# Playwright
test-results/
playwright-report/
blob-report/
